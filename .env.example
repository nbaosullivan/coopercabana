# Supabase Free Tier project credentials.
# If these are left unset, the app automatically falls back to an
# in-memory mock dataset (see lib/mockData.ts) — no errors, no setup required.

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
